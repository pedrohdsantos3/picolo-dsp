<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Necessário para OAuth, API TONE3000
        e download dos modelos.
    -->
    <uses-permission
        android:name="android.permission.INTERNET" />

    <!--
        Mantemos por causa do fluxo de áudio.
    -->
    <uses-permission
        android:name="android.permission.RECORD_AUDIO" />

    <!--
        O aparelho precisa poder atuar como USB host.
        Não é required porque queremos permitir a instalação
        mesmo em dispositivos onde o suporte possa variar.
    -->
    <uses-feature
        android:name="android.hardware.usb.host"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tone3000M1">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask">

            <!-- Launcher -->
            <intent-filter>

                <action
                    android:name="android.intent.action.MAIN" />

                <category
                    android:name="android.intent.category.LAUNCHER" />

            </intent-filter>


            <!--
                Callback OAuth:

                tone3000m1://callback
            -->
            <intent-filter>

                <action
                    android:name="android.intent.action.VIEW" />

                <category
                    android:name="android.intent.category.DEFAULT" />

                <category
                    android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="tone3000m1"
                    android:host="callback" />

            </intent-filter>

        </activity>

    </application>

</manifest>